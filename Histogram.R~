##########################
#### CREATE HISTOGRAM ####
##########################

#Load data
subjData <- read.csv("/data/joy/BBL/projects/pncPreterm/subjectData/n282_Prematurity_allData.csv", header=TRUE, na.strings = "NA")

#Create a histogram of number of subjects by the values of one NMF component for the NMF schematic.
hist(subjData$Nmf26C1,breaks=10)
